- 已完成：添加在第一次安装后启动的时候的提示，提示用户可以拖动图标导入
- 已完成：设置中的AI测试连接失败
- 已完成：右键文件夹添加解散按钮，右键图标添加删除
- 优化图标整理的思维链组件，可以参考 https://heroui.pro/docs/react/components/chain-of-thought
  - ChainOfThought（根容器，Disclosure 可折叠）
    ├── Trigger 触发器（摘要文案 + 流式动画）
    ├── Content 内容面板
    │ └── Steps 垂直时间线容器（步骤间连接线）
    │ └── Step ×N 单个步骤（状态 + 标签 + 正文，可嵌套）
- 已完成：steam的游戏应用的图包都没有获取到
- 添加壁纸后，设置页开关按钮开启后开关的圆圈变成黑色和按钮的主题蓝色背景不匹配
- 添加内置提示词，可以把常用的软件的分类内置到项目中的提示词中，前提要让用户能看到
- 尝试一些新的AI功能，比如在对话框让AI添加一个新的网页图标 在那个文件夹/底部固定图标栏 等
- 优化圆角
- 设置图标库添加多选功能
- 右键菜单添加新增文件的功能，以及在设置中添加右键删除文件是否连同源文件一同删除的开关按钮
- 现在会话中助手的回答有多个思考，但是每个思考显示的用时都是一样的，这是怎么回事，没有统计每个思考的单独计时吗，你没有参考过chatgpt的对话方式吗，chatgpt的对话是像参考图中的一样，在最顶部显示对话开始到结束的总计时，思考是不计时的，以及思考的字体不应该加粗变大，应该是和正常字体一样的，然后在所有思考和agent循环完成后会将agent循环过程输出的思考、文本、工具调用这些都是在总计时下方的，在完成后会折叠起来，可以手动点击总计时展开，折叠后会在下方显示这次对话的汇总；我描述的就是参考图中的内容，根据参考图的我的描述处理现在的ai会话
