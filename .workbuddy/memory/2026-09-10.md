# 2026-09-10

## WebView2 透明窗口撕裂修复落地（scroll 布局）

按用户提供的修复说明，将修复应用到源码（此前工作区是干净的，修复未落地）：

1. `src/styles/globals.css`：
   - `.launchpad-scroll-layout, .launchpad-scroll-layout *` 一刀切禁用 `backdrop-filter`（`!important`，含根元素自身 = `.launchpad-bg`）；分页布局不在子树内，毛玻璃保留。
   - `.scroll-grid-icon-surface` 删常驻 `will-change: transform` 与基础态 `drop-shadow`（hover 态瞬时投影保留）。
   - `.folder-drop-receiver-backdrop` 删 idle 态 `blur(0)`、transition 中 backdrop-filter 两项、`will-change` 里的 backdrop-filter；active 态 blur(20px) 保留（scroll 下被压掉）。
2. `src/components/search/SearchPanel.tsx`：搜索面板删 `will-change-opacity`。
3. `src/components/icon-grid/views/DockBar.tsx`：Dock 滚动条 thumb 删 `will-change-transform`。
4. Rust 侧确认无 `--disable-gpu-compositing` 残留（诊断开关已撤销，窗口逻辑零改动）。

验证：`pnpm build`（tsc + vite build）通过。压测脚本（13 次点击挂载面板 + 8 次 reload + 2 次激活切换）未保存在 scripts/，重启 dev 后如需复测需重建脚本。
