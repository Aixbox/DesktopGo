interface:
  display_name: "Commit Changes"
  short_description: "Run fast checks and write atomic commit messages"
  default_prompt: "Use $commit to run the bounded build and 1000-line preflight, split current changes into atomic commits, write validated messages, and commit them."
